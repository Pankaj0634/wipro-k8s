apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: pankaj-http-route

spec:
  parentRefs:
    - name: pankaj-gateway

  rules:
    - backendRefs:
        - name: pankaj-service
          port: 80
